# 26.删除有序数组中的重复项

vector 的几个常用操作

```cpp
class Solution {
public:
    int removeDuplicates(vector<int>& nums) {
        auto it = std::unique(nums.begin(), nums.end());
        nums.erase(it, nums.end());
        return nums.size();
    }
};
```